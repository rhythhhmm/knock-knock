import React from "react";
import TractionCard from "../TractionCard";
const TractionStat = () => {
  return (
    <section id="traction" className="py-24 relative z-10">
      <div className="max-w-5xl mx-auto px-6">
        <TractionCard />
      </div>
    </section>
  );
};

export default TractionStat;
